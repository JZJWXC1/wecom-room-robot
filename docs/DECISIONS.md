# 设计决策记录

本文件用于记录项目模块级设计思路、边界、取舍和验证方式。每完成一个模块后，追加 3-5 句话；如果涉及重大设计决策，应先在实现前给出 2-3 个方案对比，再把最终选择和原因记录到这里。

## 2026-07-04 文档协作规范增强

本次变更属于文档与协作流程模块，不改变 Agentic RAG、素材同步、发送阶段或线上运行行为。设计上把重大设计先比选、模块完成后留痕、平台限制与设计选择分开说明三条规则写入 `AGENTS.md` 的修改流程，使后续所有执行者在动手前就能看到它们。`CLAUDE.md` 只保留同等语义的硬规则摘要，并继续声明以 `AGENTS.md` 全文为准，避免两个入口产生冲突。新增 `docs/DECISIONS.md` 作为模块设计记录的固定落点，降低未来“要求追加但文件不存在”的执行摩擦。验证方式是文档结构检查和 diff 审阅；此变更不涉及微信、企业微信、飞书或阿里云平台限制，属于项目协作设计选择。

## 2026-07-04 QA fixture 生成器与守卫测试(P0-1 批2)

本模块属于测试基础设施,不改变线上运行行为。方案比选已由用户裁决(2026-07-04):数据源采纳 `data/inventory_cache.csv + meta` 方案(备选的服务器实时拉取方案推迟到 P0-2 部署窗口),附加三条硬要求——溯源 meta 必带 `source_snapshot_time`、生成器内置密码清洗且产物全文不得命中 `\d{4,}#`、存在性 gate 探针房号必须有守卫证明不在产物中。设计要点:fixture 本体不含时间戳,同一快照重复生成字节级一致,生成时间只进溯源 meta(`test_inventory_cache_provenance.json`);清洗规则是摘除密码 token 而非整字段置换,保留「6.19空出」类正常语义;生成前校验源 meta.hash(文件原始字节 sha256)与 status,拒绝半写入缓存。验证方式:`tests/test_qa_fixture_guards.py` 七项守卫(密码零命中/回填/溯源字段/探针缺席/列契约/确定性/哈希失配拒绝),守卫用合成源数据跑生成器逻辑,不依赖本地运行时缓存,干净检出可跑。fixture 换血与已提交产物守卫在批3 落地。此模块不涉及平台限制,属项目测试设计选择。

## 2026-07-04 QA fixture 换血与存在性双探针(P0-1 批3)

本模块把 QA fixture 从 14 行手工合成数据换血为快照生成的 40 行真实数据(source_snapshot_time=2026-07-02 15:12:23),重写索引由生成器复用生产同源 `write_rewrite_inventory_index` 一并再生,溯源 meta(provenance)与 fixture、索引三方行数/哈希交叉守卫。关键设计决策是存在性探针的双探针制:真实表存在小区「皋塘运都」,与复判报告原文"高塘运都9-402B"仅一字之差,按字面做小区级探针会在错别字被纠正时发生华丰欣苑式语义反转,故改为房号级探针(皋塘运都9-402B,断言小区存在且房号不存在)+错别字小区级探针(高塘运都整小区不存在)双保险。fixture_sha256 口径定为 LF 规范文本,生成器写盘禁用平台换行翻译、守卫重算时先规范化,对 git autocrlf 与跨平台免疫。验证方式:tests/test_qa_fixture_guards.py 十项守卫 + test_l4 实体锚点对真,全量 1275 passed。影响范围:仅测试资产与 QA 基建,不改线上行为;实测换血失败面为全量 1272 中仅 test_l4 一项,14 个引用旧实体的测试文件均为内联合成数据、不受影响。

## 2026-07-04 存在性 gate 验收窗口与机器判分(P0-1 批4)

本模块补上复判报告整改项②:验收剧本新增第 11 窗 `existence_gate_gaotang`,探针沿用批3 的双探针制(房号级皋塘运都9-402B+错别字小区级高塘运都),并在 `_turn_problem` 判分链新增 `_existence_probe_problem` 规则——探针被绑定为真实目标、或被无纠偏地确认存在/承诺发送,均判 high;正确行为(反问纠偏+给出近似真候选 16-1-2206)不受罚。设计取舍:选择加第 11 窗而非改造既有窗口,因为 10 个旧窗口各有独立回归使命;窗口数量契约收敛为 `EXPECTED_FULL_WINDOW_COUNT = len(WINDOWS)` 单一事实源,消灭三处硬编码。探针常量在 runner 与 fixture 守卫之间用测试双向锁定,守卫另加"探针房号全局不存在"断言(判分按房号 token 匹配,必须保证全表无 9-402B)。验证方式:判分规则三态单测+剧本 fixture 一致性测试+全量 1279 passed;剧本真实跑动(300/300 gate)留待 P0-2 窗口与最终验收一起执行。皋塘组候选覆盖缺口不需要新窗口——W06 剧本本就问皋塘,缺的是数据,批3 换血后该组已有 5 行真实房源。

## 2026-07-04 验收摘要声明数据出处(P0-1 批5)

本模块落地裁决 ②a 的摘要侧:验收摘要与 gate 汇总必须声明本轮 QA 实际消费的房源数据出处,`usable_for_release` 新增数据出处条件,无出处或出处失配一律不放行。关键设计决策是出处为现场核验结论而非自报字段:`build_data_provenance` 在 qa_fixture 模式下现场重算 fixture sha256(沿用批3 的 LF 规范文本口径)与行数,和溯源 meta 交叉核验后才声明 `fixture_version` 与 `source_snapshot_time`,fixture 被篡改或 meta 过期时摘要自动失效;server_cache 模式声明缓存 `synced_at`,缓存 meta 缺失或无同步时间字段即 ok=false。取舍两处:出处整轮只解析一次(同一轮消费同一份数据,逐次写盘不得漂移);gate 聚合层跳过的阶段豁免出处检查,交由 full_suite_completed 拦截,避免重复扣分。验证方式:test_qa_fixture_guards 四态出处守卫+摘要层"无出处不得放行"与 gate 层"阶段出处失配整卷不放行"负向用例,全量 1285 passed。影响范围:仅 QA 基建与测试资产,不改线上行为;裁决 ②a 至此在 fixture 溯源(批2)、产物守卫(批3)、摘要声明(批5)三层闭环。

## 2026-07-04 候选集清空与幻觉绑定收口(P0-2 部署前修复批/批6)

本模块修复 P0-2 部署预检 gate 抓到的链路缺陷:换血 fixture 首次全量跑动在 shiqiao_whole_rent 第8轮暴露"候选集被伪锚点误清(清空 bug 家族第5次复发)+待发视频单行半桶水绑定(幻觉绑定)",310 用例中唯一违规。方案比选:①再加清空/保留例外补丁——CLAUDE.md 硬规则4明令禁止(第5次复发),否决;②立即执行记忆生命周期单 owner 全量重构——正确终局但跨 kf_context_memory/resolver/main 三模块,部署窗口前风险不可控,且结构债本身要求"先固化回归用例再动刀",本批只做前置固化,重构按结构债排期(2026-07-05)单独批次;③(采纳)修错误证据+向既有契约收敛——清空决策的小区提及锚点必须命中已知小区词表(_has_vocabulary_backed_inventory_anchor,错别字经近似纠偏仍命中,存在性 gate 语义不变),resolver 移除 85e864f1 遗留的三处选择上下文旁路(待发素材须覆盖全部序号/单套 confirmed 仅满足单序号/复数选择缺候选集一律反问),不新增任何 clear 例外、不放宽判分。曾尝试修 _anchor_terms 分词伪造(空格占位),实测破坏既有小区提及契约(碎片通过2-8字过滤)已回滚,现状由测试留痕、彻底修复归重构批。口径变更一处:复数比较由单套 confirmed 绑定作答反转为 selection_error(与判分锚矛盾,判分锚优先),已登记台账。验证:新增7项回归+1项口径反转,受影响四文件 498 passed,全量 1292 passed,shiqiao 单窗 10/10 零问题,全量 gate 310/310 复跑确认(产物见 qa_artifacts)。影响范围:结构化会话记忆(清空决策)与 Planner/工具绑定阶段;客户可见文案仍全部出自受控模板层,无新增文本源。

## 2026-07-04 出站台账增量缓存与离线QA台账隔离(QA耗时热点修复批)

本模块归属发送阶段(出站幂等台账 kf_outbox)与 QA 基建(offline_guard),修复离线 QA gate 每轮耗时从 0.27s 涨到 3.45s 的性能回归。cProfile 定位(95s 采样中 91s 落在 msvcrt.locking)+ 单点量化确认双热点:其一,LocalKfOutboxLedger.reserve 每个出站动作在锁内全量重读并逐行解析共享台账 data/kf_send_outbox.jsonl(已随 QA 轮次累积到 18.8MB/16394 条,单次解析 3.0s),属生产代码 O(台账体积) 低效,与 fixture 行数无关;其二,多个 QA 进程共用该台账互抢文件锁,Windows LK_LOCK 重试约 9 秒后抛 OSError(36),导致并发 QA 整窗报错。方案比选:①(采纳)增量尾读缓存——持锁期间按字节偏移只解析新增行,文件被替换(dev/ino 变化)或变短时整体重建,损坏行含残缺尾行一律 fail-closed,文件格式/锁协议/幂等语义零变更;②台账轮转——改变幂等去重视界语义且轮转间隔内仍全量读,否决;③迁 SQLite——选库级变更需迁移比选,列为结构债候选不在本批做。隔离侧沿用 offline_guard 既有先例(KF_DIALOGUE_EVENT_LOG_PATH),KF_SEND_OUTBOX_PATH 缺省时指向每进程唯一临时路径,离线 QA 不再写共享台账。验证:新增 tests/test_kf_outbox_incremental_cache.py 八项(warm/cold 决策一致性、跨实例尾读可见、轮转重建、尾部损坏与残缺尾行 fail-closed、attempt 递增、全量重建次数白盒、隔离守卫),全量 1300 passed;shiqiao_whole_rent 单窗复跑每轮均值 3.455s→0.077s,send 阶段 3.415s→0.030s,10/10 零错误零外呼。影响范围:生产发送路径同收益(服务器为 Linux flock 无 9 秒重试症状,但全量解析同样阻塞事件循环、随台账线性变慢),本批不部署;data/kf_send_outbox.jsonl 现存 18.8MB 历史记录未清理,轮转留待运维窗口决策。

## 2026-07-04 企微回调重复投递防线(msgid 幂等三层,架构级修复移交会话)

本模块归属入站阶段(wecom_kf sync/认领)、轮次管理(_restart_kf_turn)与发送阶段(kf_outbox 域防重),修复生产实证的同 msgid 双处理双外发(16:01 事件,详见 docs/audit/kf-callback-dedup-latency-budget-20260704.md)。审计结论:回调 ACK 已是立即返回(平台 5 秒约束满足,这一点是平台限制;如何去重是本项目设计选择),缺口在幂等窗口——sync 游标临界区不串行、processed_msgids 只在 40 秒链尾写入、纯重复投递被 _restart_kf_turn 当客户追问整轮重放,且台账幂等键为轮次域设计(每轮证据链 id 掺 generation/time_ns),跨轮重放键值全变拦不住。方案比选:①(采纳)三层纵深——入站持久认领窗口(TTL 300s,失败过期可补处理)+轮次守卫(无新增 msgid 不重启)+台账 msgid 域键(msgid_scope_key 随记录落盘,SENT/UNCERTAIN/他键 pending 阻断,FAILED 放行);②仅内存去重否决(进程重启/多 worker 失效,事故已实证内存链会漏);③sync 时立即 mark_processed 否决(轮次失败消息永久丢失);④幂等键剔除轮次域否决(客户合法重复请求会被历史回执永久阻断)。实现要点:SendAction.metadata 在合同层已脱敏,域键必须取未脱敏顶层 turn_id 的 msgids: 前缀并作为白名单内部键落盘(测试抓出过按 metadata 取值导致跨 msgid 碰撞的实现错误)。验证:新增 tests/test_kf_callback_dedup.py 8 项 + tests/test_kf_outbox_msgid_scope_guard.py 7 项,关联套件(test_wecom_kf 452 项、outbox 增量缓存、入口图)全绿,全量结果登记台账。附带交付 LLM 重试链延迟预算评估(rewrite 无界是遗留风险,收敛建议留独立批次)。不部署、不 push。


## 2026-07-04 P0-2 部署与真实验收(收官记录)

本批完成 P0-2 主体:经用户会话内明确授权(视同 APPROVE_DEPLOY)部署两个 release——主部署 20260704-langgraph-hardening-150051(HEAD 7712db6d,含批2-7 全部整改与行为修复)与热修 20260704-p1video-hotfix-165500(HEAD 4cb0be89,含裁判 P1 修复、视频转码标记补配、出站台账增量缓存)。部署法=cp -a 旧 release 底座+SFTP 上传变更清单+逐文件 sha256 核验+release 内服务器全量 pytest(1287/1299 passed)+原子切 current 软链+systemd 重启+健康/定时器/无人值守三查全绿;回滚路径=软链切回旧 release(一条命令)。验收证据:离线 gate 310/310(35 行对真 fixture,usable_for_release=true,双阶段数据出处齐备,存在性 gate 首次全量通过)+服务器隔离真实对话验收 100/100(真实 LLM,1491 秒,零失败,产物已回拉 tmp/server_snapshot)。生产实测暴露并处置三问题:①企微回调 404——前任 2026-07-03 改 nginx 时误删 80 端口 /wecom/ 转发(与本次部署无关,已按备份原状恢复+注释加固);②视频 40011 后转码不触发——判定标记漏配 invalid video size(批8 修复,ffmpeg 在位);③单消息 40 秒处理引发企微回调重试与重复外发、视频话术先发后传留孤儿话术——两项架构级修复移交独立会话进行中。运维附带:停用旧版 wecom-room-aibot 崩溃循环服务(模块已不存在,5.8 万次空转,stop+disable 可逆);取证包入 .gitignore 防泄密。遗留待办:全新剧本最终验收轮与独立第二裁判终审(三方全绿收官的最后两方)、记忆生命周期单 owner 重构(结构债 2026-07-05)、回调去重与话术顺序两个进行中的修复会话的收编评审。

## 2026-07-04 视频先传后发与合并话术收敛(架构级修复移交会话,批9)

本模块归属发送阶段(main 视频发送链路与最终动作装配),修复生产实证 2026-07-04 16:06 的两个缺陷:视频话术先发后传,上传失败(40011)后客户只看到"这是XXX的视频。"没有视频(孤儿话术);LLM2 合并版话术与逐条 caption 重复,一次视频请求对客外发 3 条文本。方案比选:发送顺序上,①(采纳)拆用企微既有 upload_media/send_video_media 两接口,先传素材、成功后再发 caption+视频消息,配合确定失败补发受控模板纠正话术("XX的视频这边暂时没发出去,你稍后再让我发一次。"),send_uncertain 不补发;②仅失败后补发纠正话术不调顺序——孤儿窗口仍在,只算止血,否决。话术收敛上,①compose 层(kf_llm2_outbound)把与 caption 重复的 reply_text 置空——被 main 验收门(package_passed+非空 reply_text 硬条件,main.py compose_and_validate_llm2)连锁触发重试/受控渲染兜底,需同步松动 llm2_output_missing_visible_reply 与 L0-L3 多层口径,刚过 300/300 验收不宜大动,否决;②(采纳)发送层收敛——合并话术逐子句(去标点空白精确匹配)均被将外发的 caption 覆盖时跳过合并文本,claim/caption 契约与 production_missing_action_caption/require_captions 守卫口径零变更;③反向收敛去掉逐条 caption 只留合并文本——违反 caption 强制守卫且孤儿风险更大,否决。附带:上传阶段失败(含超时)一律记确定失败允许重放(send_uncertain 语义只留给视频消息本身);删除无调用方的 _send_videos 死代码。验证:改造+新增回归 9 项(孤儿话术消除/纠正话术/未决不补发/上传超时可重放/两链路去重与保留/子句规则),全量 1333 passed。边界:图片链路同构风险未收口(需 send_image_media 拆分,留独立批次);与回调去重批(回调去重批1/2)并行移交会话,本批经临时索引仅提交本会话变更。

## 2026-07-04 转码等待提示与原视频签名直链(用户需求批/批10)

本模块落地用户两项发送阶段需求。方案比选一(提示挂载点):①发送层确定性过程提示(采纳)——插在确认转码重试且转码缓存未命中之后、ffmpeg 启动之前,时机最准、只在真要等时打扰,沿欢迎语与批9失败纠正话术的"发送层受控常量"先例,提示发送失败不阻断视频送达(尽力而为);②发送前用9MB本地阈值预判跳过首传——省一次注定失败的大文件上传但改变 reactive 语义、observability 口径漂移,列为后续优化;③LLM2 caption 模板追加后缀——最合规但组装时点早于发送存在 TOCTOU,否决。方案比选二(链接来源,用户裁决):服务器签名直链(采纳)>飞书分享链(外部客户打不开/临时链时效极短)>小程序素材页(路由契约在另一仓库)。实现:app/services/media_link_signer.py 纯函数签名/校验(HMAC-SHA256+过期时间+路径规范形,fail-closed);GET /wecom/media/original 回源 room_database 内文件,复用既有 nginx /wecom/ 转发零配置;绑定图 _original_video_source_summary 在 manifest 无链接证据时用签名直链兜底(manifest 优先,密钥未配置回到"没有原视频链接"原口径);密钥经 APPROVE_DEPLOY 在服务器 .env 就地生成不入库不进日志。文案红线:「视频有点大,正在压缩,请稍等。」逐条核验不命中出站校验 HARD_FORBIDDEN(稍等+发)/FUTURE_SEND/IMMEDIATE 正则与 QA BAD_TEXT 表,提示常量旁注释锁死措辞禁区。验证:签名模块7项+路由4项+绑定图三态3项+转码提示3项(含缓存命中不发/重放不重发)共17项新增或更新,全量 pytest 绿。影响范围:发送阶段与媒体绑定图;客户可见新增文本均为受控常量,无新增 LLM 文本源;数据侧零新同步链路(原视频=room_database 既有源文件)。
## 2026-07-05 预算追问约束继承兜底(生产实证批/批13)

本模块修复生产实测暴露的结构性缺陷:双LLM production 模式的理解分支提前 return,constraint_proof 恒空,区域白名单只能靠"本轮原话字面含区域词"触发——任何不带区域词的追问轮全表裸搜("5000以上的有吗"搜出东站组一室一厅被措辞成"新天地的两室";离线 gate 因 rewrite 桩总产出带区域 proof 而测不到)。方案比选:①候选集内精筛扩展——方向反转型追问(5000以上)的答案必然不在旧候选集(5000以内的子集)内,数学上不可行,仅适用收窄型(既有 _refine_rows_within_candidate_context 已覆盖),否决;②LLM1 回流重试优先——契约上最纯但重试仍可能丢约束且加一轮 LLM 延迟(生产单轮已 18-40s),列为后续优化;③(采纳)确定性安全阀——只在"库存检索+proof 无范围约束+无词表校验锚点+无序号选择"时,按 LLM1 packet 的 inherited/replaced 优先、_memory_search_context 记忆兜底补齐 area/layout/budget 空槽,cleared 键与 LLM1 已给值绝不覆盖,落 constraint_inheritance_backstop 证据标记。配套把"以上/超过"补进价格解析(开区间99999哨兵),这是下游继承/精筛/记忆判定的共同前置。锚点判定复用批6 词表校验版(_has_vocabulary_backed_inventory_anchor)——纯文本启发式会把"5000以上的有吗"析出伪锚点误挡兜底,同款问题第二次出现,进一步支持词表校验作为锚点判定的统一口径。验证:7项新增回归(生产时间线固化+四负向护栏+价格三态)+全量 pytest+离线 gate 310例。影响范围:问题理解/意图继承阶段(安全阀形态,LLM1 仍是 owner)与 inventory_query 词法;出站户型矛盾拦截与离线桩故障注入窗口挂独立任务卡。

## 2026-07-05 出站户型/区域声称与证据矛盾拦截(生产实证批/批14,防御纵深)

本模块归属发送前出站校验(kf_outbound_validation),是批13(检索约束继承兜底)之上的防御纵深:同一生产实证(2026-07-04 23:58)中 LLM2 把证据行"一室一厅/东站组"措辞成"新天地的两室",出站链路对户型/区域声称是零核验(全文无任何 layout/area 检查,价格没错只是因为金额 token 恰有白名单核验)。方案比选:①compose 层 guard(kf_llm2_outbound 接入 guard_reasons)——离 LLM2 最近且走既有 failure_package 重试,但 guard 失败会连锁触发 main 验收门(package_passed+非空 reply_text 硬条件)多层口径松动,刚过 310 验收不宜大动,列为后续批次;②(采纳)出站校验 L3 新增 `l3.layout_claim_mismatch`/`l3.area_claim_mismatch`——L3 只置 requires_rewrite,走既有"一次 l3_rewrite 重试→仍失败落受控渲染器(渲染正确户型)"路径,安全降级,不触碰 facts_passed/send_allowed 属性定义;③L1/L2 blocking 级——main.py blocking 分支默认禁用受控渲染器,误伤时该轮可能无回复,且 facts_passed 语义与既有断言强耦合,否决。误伤面三防线(只拦"声明了且与证据冲突",不拦"未声明",与 llm.py 自检提示词"允许不逐字复述约束"契约一致):否定/回声句段整体豁免(暂无/没查到/你要的/按您);声称只在肯定推荐上下文(同句含证据行标签或"有一套/这套"类措辞)计入;户型比对收敛到 broad 标签(两室 对 两室一厅 按泛称包含放行,大两房经"两房"归一,厅卧一体等无映射表述宁可不报),区域按共享别名组归一(皋塘=东站组),声称词出现在证据全文(含小区名)即 fail-open。词表零新增拷贝:户型=inventory_query.ROOM_TYPE_GROUPS、区域=region_inventory_constants、行字段键=kf_dual_llm_shadow.ROW_ALIASES;支持面只取"像房源行"的证据,task_packet 约束(inherited_constraints 里的"两室")明确不算支持证据——那正是本次幻觉的来源。验证:11项新增回归(生产时间线固化+泛称/口语正反+否定回声+未声明不拦+同组别名+小区含区域词+无字段跳过+混合句只拦肯定段),全量 pytest 1362 passed,离线 gate 310/310(校验器仅 production 链路消费,离线 gate 走 shadow 分支,复跑为确认无间接回归)。影响范围:仅出站校验层与测试;不改 compose/发送/判分行为,判分层户型一致性(5w50 runner 与本地 _constraint_consistency_selfcheck 平移)与 compose 层 guard 列为后续批次。不部署、不 push。

## 2026-07-05 媒体清单发布策略修复:送达门与观察门分离(生产故障修复批/批15)

本模块归属房源/素材同步阶段,修复 wecom-room-robot-feishu-region-sync 自 07-01 19:01 起连续失败 10 次、room_database/media_manifest.json 停更于 07-01 04:15 的生产故障。取证方式为离线只读重放(tmp/audit_orphan_distribution.py,仅飞书列目录不下载不写入,房源行用服务器 07-04 快照),结果与 07-04 13:20 线上报告完全吻合:云盘 1070 个媒体文件=329 绑定+741 孤儿;741 孤儿中 739 个是"房源目录命名规范但当前房源表(35 行,只含在租房)无此行"的 122 套历史房源存量素材,2 个为哈希名杂项,0 个未知 lst_id,0 个双层重复;60 个孤儿带模糊候选(含 6 套在租房的云盘目录名与表格漂移,如"东方茂T3-1540"对表"东方茂商业中心T/3-1540")。根因是 06-30 f362ca8c 引入的发布 gate 要求 orphan/fuzzy/missing/ambiguous 全为零:素材库天然覆盖历史房源而房源表只有在租行,孤儿是稳态不是异常,任何一个孤儿都把整份清单(含已绑定的新房源翰皋名府8-1403 的图片)扣在 _manual_review 候选区永不发布,graph 阻塞、systemd 连败。触发事件是 07-01 云盘加"房源素材"父层的整理动作(服务器 video/ 双层残留即旧版镜像同步在该窗口留下;当前云盘树已无该层)。

方案比选:①(采纳)送达门与观察门分离——发布阻塞条件收敛为"下载失败非空或绑定为空"(MediaBindingReport.publish_blockers),孤儿/模糊/歧义/缺失降级为观察指标(报告+持久隔离台账 _manual_review/media_manifest_quarantine.json),清单每轮发布;安全依据是生产读取器 MediaManifestProductionAdapter 本就只放行精确绑定、非歧义、confidence>=0.99 且本地哈希核验通过的条目,孤儿从来到不了客户,全阻塞 gate 是对下游既有防线的重复,代价却是整链路瘫痪。②扩大绑定上下文(历史行注册表让历史素材也绑定)——需新增持久注册表,且 MediaManifest.__post_init__ 会把条目 listing_id 自动并入 listing_ids,历史房源会变成 send_ready,违反"房源事实只能来自最新房源表"硬规则,否决。③运维侧治理(历史素材移出同步目录,保持全阻塞)——零代码但每次退租都要人肉搬目录,业务方 07-01 恰是想把素材归档进同一棵树,与经营动线对抗,否决。

配套决策四项:a)"房源素材"包装层归一——共享常量 MEDIA_WRAPPER_FOLDER_NAMES(region_inventory_constants)+is_media_wrapper_folder(region_inventory_utils),清单 walk 与旧版镜像 _sync_folder 都视其为透明层(不进 source_path/folder_parts),再叠加 source_path 去重(duplicate_source_path 进 skipped),双层并存的迁移半途状态不再产出重复条目或双层镜像树;b) 隔离项只记台账不下载——原实现把每个孤儿文件下载进随 staging 临时目录销毁的隔离区,零留痕纯耗带宽(每轮约 70% 下载量浪费在孤儿上),改为持久 JSON 台账,失败可见;c) 已解析 listing_id 的隔离项自动复活=无状态重绑定+每轮发布的自然结果(翰皋名府8-1403 行补入后下一轮即绑定发布,已用测试固化该时间线),发布成功时清理陈旧候选区工件防运维误读;d) 行后补的增量再匹配同理,不引入增量状态机。结果契约:ok=是否发布,ready=完全干净(观察),status 新增 published_with_quarantine,blocking_count 改指发布阻塞数,新增 quarantine_count/quarantine_path。验证方式:tests/test_media_manifest.py 新增包装层透明/双层去重/发布门三态(孤儿不拦、失败拦、空绑定拦)且孤儿零下载断言,tests/test_inventory_snapshot_m1c2.py 新增缺失不阻塞发布/孤儿台账/复活时间线/候选区清理四用例,tests/test_feishu.py 新增镜像透明层用例;口径变更登记 docs/release/acceptance-criteria-change-log-20260705.md;全量 pytest+离线 gate。影响范围:房源/素材同步与运维部署,不触碰 RAG 对话链路;客户可见行为变化只有"新房源素材恢复可发"。服务器侧 video/ 双层残留清理与候选区陈旧文件清理属部署窗口运维操作,随 APPROVE_DEPLOY 执行。

## 2026-07-05 Settings 未知 env 键容忍(运维配置解耦,批15附)

07-04 19:00 失败实证:.env 先行新增 KF_MEDIA_LINK_SECRET 而代码未部署,pydantic-settings 默认 extra="forbid" 使 Settings 构造直接抛 extra_forbidden,同步进程崩溃,直到代码部署自愈。方案比选:①forbid(现状)——键名 typo 启动即炸看似可防错,但代价是"配置先行于代码"这一常规运维顺序变成致命错误,且炸点覆盖所有消费 Settings 的进程(含主服务重启窗口),否决;②(采纳)ignore——未知键静默忽略,配置与部署顺序解耦,typo 风险由字段默认值兜底并经部署后 UnattendedCheck/健康检查暴露;③allow——未知键存入模型属性空间,污染面大且无收益,否决。验证:tests/test_config_env_tolerance.py 两项(未知键不抛错、已声明键正常解析)。影响范围:全局配置层,行为变化仅"未知键从致命变忽略"。

## 2026-07-05 批14+15 部署与同步链路恢复(收官记录)

经用户 APPROVE_DEPLOY 部署两个 release:主部署 20260705-media-manifest-publish-110448(批14 出站声称拦截+批15 发布策略/包装层/Settings 容忍)与热修 20260705-graph-ready-hotfix-203936(result.ready 键语义修正,线上首轮实证 graph 消费 ready 键导致发布成功仍判 blocked,详见台账批15热修条目)。部署法沿用 cp -a 底座+SFTP 上传+逐文件 sha256 核验+release 内服务器全量 pytest(1366/1367 passed)+原子切 current+重启+健康检查;回滚=软链切回。验证闭环:手动触发同步 run ok=True/graph passed/failures=0,media_manifest.json 恢复发布(status=published_with_quarantine,blocking=0,263 条 send_ready 证据/33 房源),翰皋名府8-1403 图13+视频1 全部经生产读取器哈希核验 send_ready=True;隔离台账 _manual_review/media_manifest_quarantine.json 落盘(874 项历史素材观察项);陈旧候选区已自动清理;video/房源素材 双层残留 189 文件留档(/tmp/removed_wrapper_video_tree_20260705.list)后删除;UnattendedCheck 全绿,timer 下轮 07-06 08:00。边界说明:客户可见的真实外发需真实客户进线触发(企微客服平台限制,机器人不能主动开启会话),本轮验证到生产读取器哈希级证据为止;隔离台账内 6 套在租房源为云盘目录名与表格漂移(如东方茂T3-1540 对表东方茂商业中心T/3-1540),属数据侧待办,改名后下一轮自动绑定。

## 2026-07-05 审计后反幻觉护栏修复(批16:H1逗号分句/H2户型继承对称)

本模块修全维度审计(6维度/33agent/对抗核验)存活的两条 high。H1:出站校验 L3 分句正则不含逗号,客服高频"有A没有B"逗号句式里幻觉户型/区域随否定词落同一整段被豁免漏拦;修为逗号/顿号纳入分句,否定只豁免其所在子句。方案取舍:选"逗号纳入分句"而非"否定豁免收窄到子句级",前者一处正则改动即消除漏洞且更小,分子句后既有段级否定豁免天然降格为子句级。H2:production 约束继承兜底 layout 缺"本轮文本优先"(与 budget 不对称),户型改口追问"三室的呢"被记忆旧"两室"覆盖;修为 layout 补 _layout_from_text 优先。关键边界发现:area 无需对称——区域词是词表锚点,带区域的追问在锚点门(_has_vocabulary_backed_inventory_anchor)提前返回归 LLM1 own,到不了兜底注入;户型词不是锚点故会走到兜底,这解释了 H2 真实缺陷只在 layout,已固化边界测试。H3(L1 claim 值核验生产恒失效)另立任务:naive 回退一等属性会误伤控制类 claim(missing_target 等)致对客无回复(实证9→7项测试红),需事实字段分类设计,不带病上线。验证:新增4项回归(revert 精确变红)+全量1376 passed+离线gate 310/310。影响范围:发送前出站校验(L3户型区域)与问题理解兜底(layout继承);客户可见文本无新增来源,修复方向都是"让既有护栏对高频真实话术生效"。

## 2026-07-06 飞书房源笔记普通 docx 链接同步补漏

本模块归属房源/素材同步阶段,修复多维表 `房源笔记` 字段里普通 docx URL 没有进入飞书云盘素材同步的问题,不触碰 Agentic RAG 对话链路、Planner 或发送阶段。审计结论是:原链路只识别飞书富文本 mentionType=Docx,而线上表格有 3 条房源笔记以普通分享链接/URL 字段保存,导致这些笔记文档不会被读取 blocks、提取图片视频并同步到目标云盘;其余 7 条为空笔记,属于数据侧确实无素材。方案采纳"在 extract_docx_mentions 内兼容普通 docx URL"而非新建旁路同步脚本,因为这样能复用既有 note_documents 流程、房源目录兜底命名、去重和错误上报;标题清洗优先取 `【房源名】` 或 URL 字段 text/name,纯 URL 则留空让后续用行目录名兜底,避免把 token/整段分享文案写进文件名前缀。配套把 extract_note_links 从"收集整段含飞书域名文本"收敛为"只抽取真实飞书 URL",否则普通 docx 已同步成功后仍会因整段分享文案与干净 URL 不相等而误报 unsupported_note_links。验证方式为 tests/test_region_inventory_sync.py 增加普通分享文案、URL 字段、重复 mention 去重、纯 URL 兜底和报告链接清洗用例;本地只读线上盘点确认 33 条记录中 26 条有可同步 docx、普通链接漏项为 0,剩余 7 条确为空笔记,报告层 unsupported 为 0。


## 2026-07-06 出站 L1 结构化 claim 值反幻觉核验重启(审计 H3 收口,事实/控制 claim 判别)

本模块归属发送前出站校验(kf_outbound_validation L1),收口批16 另立的审计遗留 H3。根因:`_validate_claim_value_against_evidence` 经 `_first_legacy_value` 只从 `claim.legacy_unknown_fields` 取 value/field,而生产 LLM2 的规范 claim 走一等属性 `claim.value`/`claim.field`(kf_contracts.py:550-551,from_legacy_dict 把 known_field 落一等属性、legacy 恒空),故 `l1.claim_value_not_in_evidence` 这道"claim 声称值必须在被引证据字段里"的反幻觉核验对全部生产 claim 恒静默失效——面积/朝向/楼层等结构化字段裸奔(金额/户型/区域另有 compose 金额白名单与批14 L3 文本层兜底)。两个半成品修复的失败机理(务必避开):(1) 只让取值回退读一等属性 → 9 项测试红,控制/兜底类 claim(field=missing_target/deposit_policy/viewing_contact/selection_error,value 是话术不是事实)被当事实核验、话术不在证据 field_values → send_allowed=False → 对客无回复(比休眠更糟);(2) 再加"只对带 field_values 的证据核验"作用域 → 仍 7 项红,控制 claim 引用的证据同样带 field_values。深层根因:数据模型里事实 claim 与控制 claim 共用 claim.value/field,没有干净判别位。

方案比选:①ROW_ALIASES 字段白名单(纯方案a)——只对 field 命中已知行字段的 claim 核验;单独不成立,因控制证据的 field_values 恰会带同名状态标志键(missing_target 证据实测 field_values={error_code,missing_target:True,reason,resolution,requires_customer_room_ref}),"字段名是证据键"式判别会被这些控制字段名碰撞误触发,正是修复(2)仍红的确切机理。②给 Claim 加显式 is_fact/kind 标记由 compose 层打(方案b)——最干净的判别位,但要改契约 schema 及全部构造点(kf_llm2_outbound 两条生产路径、from_legacy_dict、序列化),面大风险高;且 compose 打标本身仍要落回同一套"按 evidence_type/field 归类"的启发式,收益不抵改契约成本,否决。③(采纳)证据结构化字段触发 ∩ ROW_ALIASES 归一门控(方案a∩c)——事实 claim 判别位=两条同时满足:其一 claim.field 经 ROW_ALIASES 归一命中已知行字段(area/layout/rent_pay1/community/room_no/utilities/candidate_number 等),控制 field(missing_target/deposit_policy/viewing_contact/selection_error)不在 ROW_ALIASES、归一为空即豁免,天然绕开控制证据 missing_target=True 状态键的碰撞;其二该行字段被引用证据 field_values 结构化承载(经 ROW_ALIASES 归一到同键)。命中才做字段作用域值核验,证据无该字段则无从比对、豁免。零 schema 变更、单一复用 kf_dual_llm_shadow.ROW_ALIASES(硬规则5,不新增同源拷贝)、无 evidence_type 清单、无新增词表。

实现要点:`_claim_fact_field`/`_claim_fact_value` 一等属性优先、legacy_unknown_fields 兜底(兼容旧形态与既有 legacy 触发测试);`_canonical_field_key` 经 ROW_ALIASES 反查规范键(惰性建表,规避模块加载期对后定义的 _normalize_fact_value 的前向引用);`_evidence_field_keys_for` 按"归一同名或同规范键"匹配证据键(中文别名如"押一付一"归一到 rent_pay1,LLM2 用中文字段名也拦得住);`_evidence_field_scoped_contains` 严格字段作用域比对,删去原 `_evidence_field_values_contain` 的整证据 haystack 全域回退——否则"把 area 的值安到 layout 字段"这类字段错配会被别的字段值误放行。误伤边界:控制/兜底话术 field 不在 ROW_ALIASES 豁免;证据无该结构化字段豁免;candidate_number 在 ROW_ALIASES 且证据承载则正常核验,与既有 L2 `l2.candidate_number_out_of_range`(集合越界)互补(值与其证据行一致性)。

验证方式:新增 tests/test_kf_outbound_validation.py 两项——`test_l1_claim_value_check_blocks_fabricated_first_class_fact_values`(一等 schema 伪造 area/rent_pay1/值搬错字段/中文别名押一付一 四态必拦 + area/rent_pay1/layout/押一付一 真值放行)、`test_l1_claim_value_check_exempts_control_claims_with_matching_flag_key`(控制 claim field=missing_target 即便引用证据带 missing_target=True 状态键仍豁免,守住 9 项红家族);测试真实性反转——临时把 `_claim_fact_value` 退回 legacy-only(复现审计前 dead-check),正向拦截用例精确变红,还原后复绿。回归:tests/test_kf_llm2_outbound.py + test_kf_dual_llm_production.py + test_kf_outbound_validation.py 100 passed(含 missing_target/deposit_policy/viewing_contact/candidate_number 全部控制用例仍绿),全量 pytest 1379 passed/1 skipped,离线 gate 310/310(fixed 110 + random 200 / 31 窗口,high=0/medium=0/fallback=0/network=0,usable_for_release=True)。离线 gate 虽走 shadow 分支,但经 validate_prepared_outbound_package 消费同一 `_validate_claim_value_against_evidence`,对本修复是有效回归面(区别于批13/14 只在 production 分支、离线 gate 测不到)。影响范围:仅出站校验 L1 与测试,不改 compose/发送/判分行为,客户可见文本无新增来源;修复方向是"让既有 L1 反幻觉核验对生产规范 claim 真正生效"。不部署、不 push,待用户 APPROVE_DEPLOY。
